# Work Notes

- Reviewed previous commit and aligned with follow-up request: do not include/stub `rsvp-tool-research` in this branch since it is already merged on master.
- Removed `rsvp-tool-research/try/*` from this branch.
- Removed generated checked-in copy under `docs/subprojects/rsvp-tool-research/*`.
- Kept generated data file (`docs/_data/subprojects.json`) as an empty list by default.
- Confirmed generation still works by running the script (reports zero projects in this branch state).
- Ran `git diff --check` for whitespace/hygiene.
